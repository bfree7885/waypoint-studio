# SignalTerrain V1.2 — Northeast Summit Catalog Expansion

**Status:** **NOT SHIPPED** · data source found · redistribution permission pending · importer prepared · product still V1.1  
**Runtime:** `/apps/summit-signal/` (unchanged V1.1)  
**Branch:** `cursor/summit-signal-v0-1-9f7b`  
**Acquisition:** [`V1.2-DATA-ACQUISITION.md`](V1.2-DATA-ACQUISITION.md)  
**Importer:** [`V1.2-STATIC-CATALOGUE-IMPORT.md`](V1.2-STATIC-CATALOGUE-IMPORT.md)

V1.2 coverage has **not** started as a product change. Neighboring packs are not loaded. High Point is not in the running catalogue. The unlisted field-test host remains the V1.1 build (`b7e0fffa`).

Preparation work adds an **offline** CSV importer that stages packs from a manually supplied official summit list. It does not fetch, scrape, register, or deploy.

> Visible map area and loaded summit catalogue are not assumed to be the same thing.

> Visible map area and loaded summit catalogue are not assumed to be the same thing.

---

## Triggering field observation

V1.1 made coverage **honest**. The deployed app reports that only Greater Catskills (W2/GC) is loaded, draws a dashed hull, and warns that neighboring regions around Milford, Pennsylvania and High Point, New Jersey are not loaded.

That is correct. It is not yet useful enough for home-area planning from Milford. High Point appears in the user’s existing SOTA application and still does not appear as a SignalTerrain summit marker.

V1.1 solved honesty. V1.2 was meant to solve **practical coverage**. It cannot, until a permissible pack source exists.

---

## Inspection (V1.1 architecture)

| # | Item | Finding |
| --- | --- | --- |
| 1 | Branch | `cursor/summit-signal-v0-1-9f7b` |
| 2 | HEAD at inspection | `53596ba1` |
| 3 | Working tree | Clean vs origin (accidental `V1.1.md` local hunk restored) |
| 4 | Regional-pack architecture | Manifest → packs → `validatePack` → normalize + provenance → merge/dedupe by SOTA reference → convex hull → search/map/coverage UI |
| 5 | W2/GC pack | `apps/summit-signal/data/ss-summits-w2-gc.json` (118 records) |
| 6 | Normalized schema | `{ id, name, reference, points, elevationM, elevationFt, lat, lng, maidenhead*, association*, region*, valid, seasonalBonus, sourceUrl, retrievedAt, packId, provenance, visual }` — missing source fields stay null/unavailable |
| 7 | Catalogue loader | `apps/summit-signal/js/ss-sota-provider.js` `loadCatalog()`; default `data/ss-summit-catalogue.json`; optional `?live=1` replaces **only** W2/GC |
| 8 | Pack registry | `apps/summit-signal/data/ss-summit-catalogue.json` — one entry, `W2-GC` |
| 9 | Deduplication | Merge by SOTA reference; agreeing coordinates keep one; conflicting coordinates throw |
| 10 | Coverage boundary | Convex hull of **actual loaded markers**, not the SOTA region envelope |
| 11 | Search | Name, SOTA reference, region, association across loaded packs; miss = “Not found in current summit catalogue” |
| 12 | Viewport filtering | `summitsInBounds` + hull intersection → inside / partial / outside |
| 13 | Provenance | Per-pack `source` + per-summit `provenance` (provider, URL, association, region, retrievedAt, packId, developmentFixture) |
| 14 | Pack validation | Provenance, association, region, unique refs, valid lat/lng, elevation |
| 15 | Import/preparation tooling | At inspection: none. Now: offline `scripts/signalterrain/import-sota-summits.mjs` (staging only; not registered) |
| 16 | Origin of 118 W2/GC records | Labeled development fixture, retrieved **2026-09-04** from `GET https://api2.sota.org.uk/api/regions/W2/GC`. Association W2 “USA - NJ / NY”, region GC “Greater Catskills” |
| 17 | Neighboring summit datasets | **None** in this repository (git history of `ss-summits*` is only W2/GC) |
| 18 | Permissible-acquisition docs | `docs/signal-terrain/V0.1.md` (SOTA API ToS / commercial licence gate); `docs/signal-terrain/V1.1.md` (do not expand live API; owner-approved one-time fixture retrieval as next human step) |

Planning support remains separate: only Slide `W2/GC-001` and Hunter `W2/GC-002` have access/route/AZ fixtures. Vly Mountain `W2/GC-062` is in the W2/GC catalogue; access/hike/AZ stay honestly unavailable. V1.2 must not fabricate planning data.

---

## Acquisition gate

### Candidates examined

#### A. Deterministic data already in the repository

**Not sufficient.** Only `W2-GC`. High Point State Park / High Point, New Jersey is not among the 118 records. The Catskills peak **High Point B** (`W2/GC-015`) is a different summit.

#### B. Previously retrieved/static data already available to the project

**Not sufficient.** The only previously retrieved SOTA region fixture is W2/GC (2026-09-04). No neighboring-region JSON exists in git history.

#### C. Downloadable/public dataset whose terms permit intended development use

**SOTA public API (`api2.sota.org.uk`)** is the dataset used for W2/GC.

| Question | Assessment |
| --- | --- |
| Source | Summits on the Air API, `https://api2.sota.org.uk` |
| Access method | Documented HTTP JSON endpoints (V0.1 used `GET /api/regions/W2/GC`) |
| License/terms | [SOTA API Terms of Service](https://api2.sota.org.uk/docs/index.html), reviewed 2026-09-06 |
| Automated retrieval permitted? | **Not for AI-generated software without prior SOTA approval.** ToS: “No AI or Vibe-coding” — *no AI-generated software may connect to the SOTA API without prior approval.* Also: reasonable public usage tolerated; **no commercial usage** without a formal licence; designated point of contact / Reflector `API-consumers` group required |
| Redistribution in this unlisted development catalogue? | W2/GC already exists as a labeled development fixture with attribution. Expanding that pattern is a **human / SOTA-approved** act, not an AI runtime or AI retrieval |
| Commercial/public use | Separate SOTA licence. Not assumed. This app is still unpublished / unlisted |
| Date/version of terms review | 2026-09-06, `https://api2.sota.org.uk/docs/index.html` |

V1.2 instructions independently forbid solving this by connecting SignalTerrain to the live SOTA API, scraping SOTA/SOTLAS/third-party apps, bypassing access controls, or adding unauthorized runtime requests.

This agent therefore **must not** call `api2.sota.org.uk` to retrieve neighboring regions.

#### D. Other permissibly licensed geographic source

**OpenStreetMap** (ODbL) has a documented tag `communication:amateur_radio:sota` for SOTA references. That is OSM mapper data, not a SOTA API call.

It is **not sufficient for V1.2**:

- OSM’s High Point, New Jersey peak node does **not** carry a SOTA reference tag (public OSM node tags: name, `ele`, GNIS, Wikidata; no `communication:amateur_radio:sota`).
- V1.2 requires the exact High Point SOTA identity from a legitimate catalogue. OSM does not provide it.
- OSM tagging of SOTA peaks is incomplete relative to an official region list. Using it would invent a false sense of coverage and still fail High Point acceptance.

**Wikidata** has no dedicated SOTA identifier property found in this review. Wikipedia/GNIS describe High Point as a geographic peak, not a SOTA catalogue record. Geographic names and elevations **must not** be mapped to invented SOTA references.

#### E. Manually supplied records

**None supplied** with this request. No attached JSON/CSV packs, no owner-provided High Point record.

---

## Gate decision

**Coverage: STOP. Redistribution permission pending.**

The official static summit list is identified. This agent still must not download it, call the SOTA API, scrape, or invent High Point. An offline importer can consume a **manually supplied** local CSV into **staging** only. Real packs must not be registered until redistribution permission is confirmed.

V1.1 honesty is preserved. Practical Milford-area coverage is **not** solved. That is the correct failure mode until permission arrives.

---

## High Point

**The repository / permissible source still does not contain a SOTA summit record for High Point State Park / High Point, New Jersey.**

| Field | Value |
| --- | --- |
| Summit name | Not present |
| SOTA reference | Unknown (not in-repo; not guessed) |
| Association / region | Unknown (not in-repo; not guessed) |
| Latitude / longitude | Not a SOTA record in-repo |
| Elevation / points | Unknown |
| Provenance | None |

Do not treat **High Point B** `W2/GC-015` as High Point, New Jersey.

Association W2 is labeled “USA - NJ / NY” in the W2/GC fixture, which shows NJ and NY share that association. The **specific SOTA region pack** that contains High Point State Park is **not listed in this repository**. Pennsylvania may belong to a different association; that is **not confirmed** from in-repo data. Region codes will not be guessed.

---

## Smallest safe next step (human, not this agent)

1. Confirm **redistribution** permission for the official static summit list in this unlisted development catalogue.
2. Manually place `summitslist.csv` on disk (do not ask the importer or an AI client to download it; do not call the SOTA API).
3. Dry-run `scripts/signalterrain/import-sota-summits.mjs --input … --dry-run`. Record SHA-256 and actual association/region codes from the file.
4. Identify High Point NJ and the minimum Milford-envelope regions **from that listing**.
5. Generate those packs into `tmp/signalterrain/generated-summit-packs/` and review.
6. A later V1.2 **coverage** task (not this preparation) unlocks promotion, registers packs, and republishes the unlisted host.

Do not scrape websites. Do not enable default `?live=1` for new regions. Do not ship a hidden refresh.

---

## What was not done

- No product-code change (catalogue, map, search, coverage, access, AZ, routes unchanged)
- No new production packs
- No High Point marker
- No live SOTA API usage
- No scraping
- No official CSV committed
- No field-test republish
- No V1.3

Importer preparation: see [`V1.2-STATIC-CATALOGUE-IMPORT.md`](V1.2-STATIC-CATALOGUE-IMPORT.md). Staging packs are not product packs.

---

## Deployment

Unlisted field-test URL remains the V1.1 host:

`https://bfree7885.github.io/waypoint-studio-site/apps/summit-signal/`

Stamped product SHA: `b7e0fffa`. Do not republish for importer preparation.
