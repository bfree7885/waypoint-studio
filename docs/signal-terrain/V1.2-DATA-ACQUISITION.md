# SignalTerrain V1.2 — Data acquisition

**Status:** DATA SOURCE FOUND · **REDISTRIBUTION PERMISSION PENDING**  
**V1.2 coverage:** **not shipped** · product remains V1.1  
**Preparation tooling:** [`V1.2-STATIC-CATALOGUE-IMPORT.md`](V1.2-STATIC-CATALOGUE-IMPORT.md)

Do not describe V1.2 as complete. Honest empty-map copy (V1.1) is not the same as useful Milford-area coverage (V1.2).

---

## Gate (updated)

| Question | Result |
| --- | --- |
| Is a labeled official summit dataset identified? | **Yes** — static `summitslist.csv` |
| May this agent / importer download it? | **No** |
| May SignalTerrain call the live SOTA API for neighboring regions? | **No** (ToS + product rule) |
| May we scrape SOTA / SOTLAS / third-party apps? | **No** |
| May we invent High Point or infer SOTA references? | **No** |
| May we redistribute the official list (or derived packs) in the unlisted field-test catalogue? | **Not yet confirmed** |
| May we register generated packs in the running app? | **No — stop at REVIEW** |

---

## Source

| Field | Assessment |
| --- | --- |
| Source | Official static SOTA summit list, filename `summitslist.csv` |
| Access method | **Manual user-supplied local file** after a human download |
| Automated retrieval by importer | **Not permitted** (tooling refuses URLs and performs no HTTP) |
| License / terms | **Not confirmed for redistribution or public-app use** |
| Commercial / public Waypoint Studio use | Separate approval; not assumed |
| Date of this assessment | 2026-09-06 |

The SOTA API remains out of bounds for AI-generated clients without prior approval (ToS reviewed 2026-09-06). V1.2 must not use it as the acquisition path.

OpenStreetMap SOTA tags remain insufficient (High Point NJ peak node has no SOTA reference tag).

---

## What exists in-repo

- Production catalogue: **W2/GC only** (118 records), V1.1 fixture from 2026-09-04 API retrieval.
- Importer: `scripts/signalterrain/import-sota-summits.mjs` (offline, staging only).
- Synthetic test CSV: `automation/fixtures/signalterrain/sota-summitslist-synthetic.csv` (`XX/…` refs only).
- No official `summitslist.csv` is committed.
- No real generated regional packs are registered.

---

## Remaining blocker

**SOTA / static-data redistribution permission** for using official summit-list records (and derived regional packs) in this unlisted development catalogue.

Until that is confirmed in writing:

- do not commit the official CSV
- do not commit real generated packs
- do not change `ss-summit-catalogue.json`
- do not republish the field-test host
- do not begin V1.2 coverage implementation or V1.3

---

## Next action once permission arrives

1. Human places the permitted `summitslist.csv` on disk (not via the importer).
2. Dry-run the importer; record SHA-256, region list, High Point row, nearby PA/NJ/NY codes **from the file**.
3. Generate the minimum staging packs for the practical Milford envelope.
4. Review; then a later V1.2 **coverage** task registers packs and deploys the existing unlisted github.io host.

See [`V1.2-STATIC-CATALOGUE-IMPORT.md`](V1.2-STATIC-CATALOGUE-IMPORT.md) for commands.
