# SignalTerrain V0.9

**Status:** Isolated field-test **publish is blocked**. Implementation is on this branch. **No live HTTPS field-test URL.** Not a public product launch. Not merged to `main`.

**Runtime:** `/apps/summit-signal/` (do not occupy `/apps/signalterrain/`).

SignalTerrain V0.9 is a field-test deployment, not a public product launch.

The field-test URL is unlisted, not access-controlled. (Intended posture. **No URL is live.**)

**Intended FIELD-TEST URL (not live):** `https://bfree7885.github.io/waypoint-studio-site/apps/summit-signal/`

---

## Purpose

Make accepted V0.8 usable on a real phone over HTTPS for personal SOTA field testing — without a public product launch, without a `main` merge, and without touching Waypoint Studio production or Sheds.

---

## Hosting architecture (chosen)

Inspection (commit `af0c1425`) found no Studio preview host. `pages.yml` publishes `main` only to `https://waypointstudio.org`. Creating a *new* GitHub repository returned **403**.

The inspection’s recommended isolated architecture is a **companion GitHub Pages project serving only `apps/summit-signal/` on a github.io URL** — the Sheds companion-repo pattern, not `sheds-site`, and not a Studio production overwrite.

This increment implements that architecture against the existing isolated Pages project **`bfree7885/waypoint-studio-site`** (no CNAME, HTTPS github.io, not production Studio, not Sheds):

| Item | Value |
| --- | --- |
| Provider | GitHub Pages (legacy / branch `main`) on `waypoint-studio-site` |
| Intended app URL | `https://bfree7885.github.io/waypoint-studio-site/apps/summit-signal/` |
| Custom domain | **None.** Must not add a CNAME. |
| Production Studio | Unchanged. `pages.yml` still `main` → `waypointstudio.org`. |
| Sheds host | Must not be used. `sheds-site` can receive this PAT; V0.9 refuses it. |
| Authentication | **None.** Unlisted public Pages URL, if published. |
| `X-Robots-Tag` | Not available on GitHub Pages for this project. |

Prepare/publish scripts and a dispatch-only workflow are in this repo. Do not merge to `main`. Do not dispatch `.github/workflows/pages.yml` from this branch.

---

## Blocker (STOP)

**Publish to the companion host failed: the available deploy token can write only to `bfree7885/sheds-site`.**

Verified 2026-09-05 from this agent:

| Action | Result |
| --- | --- |
| `POST /user/repos` (new companion repo) | **403** Resource not accessible by personal access token |
| Git blob create on `waypoint-studio-site` | **403** Resource not accessible by personal access token |
| Git blob create on `waypoint-studio` | **403** (App token still pushes this feature branch; PAT does not write git objects here) |
| Git blob create on `sheds-site` | **201** — **forbidden target** for SignalTerrain |
| `git push` to `waypoint-studio-site` `main` | **403** Permission denied |

Hard-stop matches: **secrets/credentials that are not already available** (contents:write on a non-Sheds companion Pages repo). Using `sheds-site` / ShedHunting.org is forbidden. Merging to production `main` or dispatching `pages.yml` is forbidden.

Owner unblock (pick one):

1. Grant the existing deploy token **contents:write** on `bfree7885/waypoint-studio-site`, then from this branch: `node scripts/prepare-signalterrain-field-test-host.mjs && node scripts/publish-signalterrain-field-test-host.mjs`
2. Create empty public repo `bfree7885/signalterrain-field-test`, enable GitHub Pages on `main` / root, grant contents:write, set `SIGNALTERRAIN_FIELD_TEST_HOST_REPO=bfree7885/signalterrain-field-test`, then run the same commands (and update the documented github.io path).
3. Do **not** treat a `main` merge unlisted path as implied.

Until then, V0.9 is **not complete**. There is no phone HTTPS URL.

---

## Public vs unlisted vs private

| Term | This host |
| --- | --- |
| Private / authenticated | **Not available** on GitHub Pages. Do not call a Pages URL private. |
| Unlisted | Omitted from Studio nav, sitemap, and homepage. Anyone with the URL can open it. |
| noindex / robots | Crawler hint only. Not access control. |

---

## Indexing protections (ready on this branch / in the artifact)

Companion artifact (when publish is allowed):

- App and host root: `<meta name="robots" content="noindex, nofollow">`
- Host `robots.txt`: `Disallow: /`
- No sitemap
- Host root is a stub, not a Studio homepage
- No CNAME

Source branch (`cursor/summit-signal-v0-1-9f7b`, not on production Pages):

- `apps/summit-signal/index.html`: `noindex, nofollow`
- `robots.txt`: `Disallow: /apps/summit-signal/`
- `sitemap.xml`: no summit-signal URL
- Homepage / primary nav / About / Support: no SignalTerrain SOTA product card

Cyber SignalTerrain remains at `/apps/signalterrain/` as a silent redirect. That is a different product.

---

## Build identification (in the app; stamped at prepare time)

- Visible: `SignalTerrain V0.9 · <short SHA>` (`#ss-field-test-build`)
- Meta: `<meta name="signalterrain-build" content="V0.9 <full SHA>">`
- JSON: `data/st-field-test-build.json` in the artifact

SHA is the **waypoint-studio source commit** baked at prepare time.

---

## Network dependency matrix (V0.8 behavior; unchanged)

| Surface | Kind | Default field-test behavior |
| --- | --- | --- |
| Application shell (HTML/CSS/JS) | STATIC FIXTURE (same-origin) | Required to load the app |
| Leaflet | STATIC FIXTURE (same-origin) | Required for the map |
| Summit catalogue | STATIC FIXTURE | `data/ss-summits-w2-gc.json` unless `?live=1` |
| OSM access | STATIC FIXTURE | Slide/Hunter fixtures unless `?access=live=1` |
| Pedestrian route | STATIC FIXTURE | Slide/Hunter fixtures unless `?route=live=1` |
| Elevation profile | STATIC FIXTURE | Matching elev fixtures unless `?route=live=1` |
| AZ terrain | STATIC FIXTURE | Slide/Hunter DEM fixtures unless `?az=live=1` or `?route=live=1` |
| Basemap tiles | LIVE EXTERNAL (Esri) | Always fetched when the map is shown. NETWORK REQUIRED for the map background. |
| IBM Plex fonts | LIVE EXTERNAL (Google Fonts) | Optional for layout |
| GPS | Browser API | Opt-in; not required |
| Copy Plan / coordinates | SESSION/MEMORY + clipboard | Local |
| Personal checklist | LOCAL STORAGE | Per summit, local |
| Session catalogue cache | SESSION/MEMORY (`sessionStorage`) | Local |

Do **not** enable live modes on a normal field-test URL (`?live=1`, `?access=live=1`, `?route=live=1`, `?az=live=1`).

---

## Maps handoff

Open in Maps uses the **selected start coordinate** (parking/trailhead), never the summit, AZ entry, nearest road, or polygon center.

| Platform | URI |
| --- | --- |
| iOS | `https://maps.apple.com/?ll={lat},{lng}&q={label}` |
| Android | `geo:{lat},{lng}?q={lat},{lng}({label})` |
| Other | `https://www.google.com/maps/search/?api=1&query={lat},{lng}` |

---

## Publish / rollback (when write access exists)

From this branch (does **not** merge to `main`):

```bash
node scripts/prepare-signalterrain-field-test-host.mjs
node scripts/publish-signalterrain-field-test-host.mjs
```

Optional Actions dispatch: `.github/workflows/signalterrain-field-test-host.yml` (`workflow_dispatch` only).

| Rollback target | Action |
| --- | --- |
| Restore pre-SignalTerrain `waypoint-studio-site` snapshot | After first successful publish, tag `legacy-waypoint-studio-site-pre-st-v09` is created. Restore that tree onto `main` with a new commit. Do not force-push unless the owner explicitly asks. |
| Studio production | Unchanged. If `pages.yml` were ever dispatched by mistake: `workflow_dispatch` `.github/workflows/pages.yml` on `origin/main`. |
| Sheds | Do not roll back `sheds-site`. V0.9 does not publish there. |
| DNS | None. No CNAME and no registrar change. |

**No V0.9 companion-host deployment occurred.** `waypoint-studio-site` remains `c45abc7e` (historical Studio snapshot). Production remains `origin/main` via `pages.yml`.

---

## Collision / Sheds / Studio production

- Waypoint Studio homepage, nav, and sitemap in this source tree were not given SignalTerrain product cards.
- `origin/main` / `waypointstudio.org` were not published from this branch.
- Shed Hunting source, `shedhunting.org`, Shed DNS, and `shedhunting-host.yml` were not modified.
- Retired cyber SignalTerrain (`/apps/signalterrain/`) was not modified.
- `waypoint-studio-site` was **not** overwritten (push denied).

---

## Phone / Slide / Hunter acceptance on a deployed host

**Not executed.** There is no real HTTPS field-test URL.

V0.8 acceptance remains valid on localhost/CI fixtures only.

---

## Known limitations

- Companion-host **contents:write** is missing for this agent.
- Unlisted is not authentication.
- GitHub Pages cannot set `X-Robots-Tag`.
- Esri basemap and Google Fonts need network even when SOTA/OSM/Valhalla/3DEP stay on fixtures.

---

## Smallest safe next action

Owner: grant **contents:write** on `bfree7885/waypoint-studio-site` (or a new empty github.io companion repo). Then republish from this branch with the scripts above. Do not merge to `main`. Do not begin V1.0. Do not publicly launch SignalTerrain.
