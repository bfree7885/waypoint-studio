# Phase 2 GIS samples (placeholder)

This folder is reserved for **small** public-data samples used during future implementation spikes.

Phase 2A recon intentionally **did not** commit statewide rasters or large extracts.

When samples are added later:

- Prefer tiny AOI clips (e.g. &lt; 2 MB) with source attribution
- Record provider, date, license, and bbox in a sidecar `.txt`
- Never commit CONUS mosaics or full-county lidar
