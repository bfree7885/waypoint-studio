# Private operator surfaces

Full Live Engine status and debug HTML are written here by `scripts/waypoint-live-engine.mjs`.
They are excluded from the GitHub Pages artifact (`private/` is removed before upload).

Public `/status.html` and `/debug.html` are intentional non-sensitive stubs only.
