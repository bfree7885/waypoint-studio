# Week Away — Environment (optional)

Copy to `.env` for local cron webhook trigger:

```bash
cp automation/week-away/.env.example automation/week-away/.env
```

```env
# Cursor Automation webhook URL (from automations settings)
WEBHOOK_URL=
```

`.env` is gitignored — never commit secrets.
