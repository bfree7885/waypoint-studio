/* Photo Coach CSS under apps/photo-coach/css/
 *
 * studio-shell.css and photo-coach.css were vendored from
 * apps/waypoint-scenes/css/ in Turnaround Sprint 5 so the live Coach
 * surface does not depend on the legacy monolith stylesheet paths.
 *
 * Coach still loads many analysis scripts from apps/waypoint-scenes/js/
 * (JS coupling remains). Prefer editing the local CSS copies for Coach
 * chrome; sync intentionally if the legacy studio needs the same change.
 */
