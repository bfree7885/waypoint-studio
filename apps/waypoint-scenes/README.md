# Legacy Scenes URL

`/apps/waypoint-scenes/` is a permanent compatibility redirect to the canonical
Scenes application at `/apps/scenes/`.

Do not add a second interactive Scenes implementation here.
